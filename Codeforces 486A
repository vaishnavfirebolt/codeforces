import java.util.Scanner;
public class Codeforces486A {
    public static void main(String args[]) {
        Scanner scan = new Scanner(System.in);
        long x = scan.nextLong();
        System.out.println((x & 1) == 0 ? x/2 : (x-1)/2 - x);
    }
}
